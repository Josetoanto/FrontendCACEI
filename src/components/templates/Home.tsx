import React from 'react';
import Header from '../organisms/Header';

const Home: React.FC = () => {
    return (
        <div>
            <Header></Header>
            <h1>Inicio</h1>
        </div>
    );
};

export default Home;
